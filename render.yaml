services:
  - type: web
    name: happytree-workbook
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: CLAUDE_API_KEY
        sync: false
